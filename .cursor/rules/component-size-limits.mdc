---
description: 
globs: 
alwaysApply: false
---
# 组件大小限制指南

本规则定义了项目中组件的最大行数限制，以保持代码的可维护性和可读性。

## 组件行数限制

根据组件的职责和复杂度，我们对不同类型的组件设定了不同的行数限制：

- **页面组件**: 最大 150 行
  - 组织多个子组件的页面级组件

- **容器组件**: 最大 120 行
  - 管理状态和数据流的容器组件

- **UI 组件**: 最大 80 行
  - 只负责渲染 UI 的展示型组件

- **工具组件**: 最大 50 行
  - 一些小型的、复用性高的组件

## 超出限制的处理方法

当组件超出行数限制时，考虑以下重构方法：

1. **分离关注点**：将不同功能拆分为独立组件
2. **提取共用逻辑**：将重复逻辑提取到 hooks 或工具函数中
3. **使用组合模式**：通过组合小型组件构建复杂 UI

## 例外情况

在某些特殊情况下，组件可能需要超过行数限制，此时应当：

1. 在组件顶部添加注释说明超出限制的原因
2. 在可行的情况下，仍然尝试将部分功能抽离为子组件

## 行数计算标准

行数计算不包括：
- 空行
- 注释行
- import 语句

主要计算实际的代码逻辑行数。
